# Agent config — devcontainer CLI host-exec surface

CLI: /workspaces/.codespaces/bin/node_modules/@microsoft/vscode-dev-containers-cli/dist/node/devContainersCLI.js (MISSING bytes)

## Config fields that run commands (where: HOST vs CONTAINER)
### initializeCommand

### onCreateCommand

### updateContentCommand

### postCreateCommand

### postStartCommand

### postAttachCommand

### postCreateCommands

## cliHost.exec call sites (host exec channel) — what args?

## dockerExec call sites (container exec channel) — count

## Does anything run initializeCommand outside container-create?
