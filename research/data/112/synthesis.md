# Synthesis — 112 agent-hijack (container → host)

To be filled after the 5 agent lanes (recon, spec, history, physics, philosophy) produce outputs in this folder.

Collect:
1. Recon: exhaustive RPC→host-exec surface map (verdicts USABLE/DEAD).
2. Spec: fuse-driver attack shapes (top 3 testable).
3. History: devcontainer rebuild chain — any host-side exec influenced by our config.
4. Physics: the single lever ("If we change X, we escape") + host locations for an add-capable SAS.
5. Philosophy: decisive next experiment, scoped to our own environment.

Then write the entry to entries/112-agent-hijack-control-plane.md (extend it) and update findings/patterns.md.
